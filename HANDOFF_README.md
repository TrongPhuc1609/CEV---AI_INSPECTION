# AI Inspection Handoff

Start every new session by reading `PROJECT_CONTEXT.md`.

Current baseline: v0.5.0.
Next task: complete v0.6.0 Rule.cmd v1.0 parser + typed InspectionPlan + validation + Orchestrator integration + tests.

Do not treat v0.6.0 as complete until the acceptance criteria in `PROJECT_CONTEXT.md` are satisfied.
