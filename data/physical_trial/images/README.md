# Real Trial Images

Place human-reviewed real PCB images here for offline replay.

Do not use this directory as a substitute for ground truth. Every image used in acceptance scoring must be referenced by `data/physical_trial/manifest.jsonl` with `GOOD` or `NG` ground truth.
